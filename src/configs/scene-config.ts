import { SceneConfig } from '../types';
import landscapeProgramConfig from './landscape-program-config';

const config: SceneConfig = {
  programs: [landscapeProgramConfig],
};

export default config;
